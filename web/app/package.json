{
  "name": "hush-web",
  "version": "0.2.0",
  "private": true,
  "description": "Browser WASM mel spectrogram and VAD demo for Hush.",
  "scripts": {
    "build": "make build",
    "deploy": "npm run build && cd ../.. && npx wrangler deploy",
    "serve": "node server.js",
    "start": "npm run build && npm run serve",
    "test": "node --test test/*.js"
  }
}
